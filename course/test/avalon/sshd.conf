# What the SSH exercise server must allow for Avalon: password logins, with
# which "Hello SSH" starts, and ExposeAuthInfo, which tells the dock whether a
# student logged in with their key.
PasswordAuthentication yes
KbdInteractiveAuthentication no
ExposeAuthInfo yes
